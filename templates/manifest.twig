{
  "dir": "ltr",
  "lang": "en",
  "name": "{{ site.name }}",
  "description": "Eternium leaderboards",
  "icons": [],
  "display": "standalone",
  "orientation": "portrait",
  "start_url": "{{ abs_url().withQuery('from=webapp') }}",
  "theme_color": "{{ site.theme }}",
  "background_color": "{{ site.background }}"
}